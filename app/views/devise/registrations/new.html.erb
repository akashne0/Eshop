<section class="login-block">
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-12">
                <%= form_for(resource, as: resource_name, url: registration_path(resource_name),class:"md-float-material form-material") do |f| %>
                    <div class="auth-box card">
                        <div class="card-block">
                            <div class="row">
                                <div class="col-md-12">
                                    <h3 class="text-center heading" >Sign Up</h3>
                                    <%= render "devise/shared/error_messages", resource: resource, class: "form-control" %>
                                 </div>
                            </div>
                            
                            <div class="form-group form-primary">
                                <%= f.text_field :name, class:"form-control",placeholder:"Display name"%>
                   
                            </div>

                            <div class="form-group form-primary">
                                <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder:"Email" %>
                               
                            </div>

                            <div class="form-group form-primary">
                                <%= f.password_field :password, autocomplete: "new-password", class: "form-control",placeholder:"Password" %>
                            </div>

                            <div class="form-group form-primary">
                                <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control",placeholder:"Repeat password"  %>
                                                              
                            </div>

                            <div class="row">
                                <div class="col-md-12">                                  
                                  <%= f.submit "Signup Now" , class: "btn btn-primary btn-md btn-block waves-effect text-center col-md-12" do %>
                                      <i class="fa fa-lock" aria-hidden="true"></i>
                                  <% end %>
                                </div>
                            </div>

                            <div class="or-container"><div class="line-separator"></div> <div class="or-label">or</div><div class="line-separator"></div></div>

                            <%= render "devise/shared/links" %>
                                                                                 
                            <br>

                            <p class="text-inverse text-center" >Already have an account? <a href="<%= new_session_path(resource_name)%>" >Login</a></p>
                        </div>
                    </div>
                <% end %>
                </form>
            </div>
        </div>
    </div>
</section>




